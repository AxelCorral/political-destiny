# Captures de l’audit UX

Ces images sont produites avec Chromium et `playwright-cli` contre l’application locale. Le nom du
fichier indique le parcours et la taille de fenêtre. Les captures longues servent à objectiver la
hauteur de défilement ; les captures de viewport montrent l’état réellement visible sans défilement.
